# Superseded: Shell-Derived Partition Implementation Status

This dated handoff has been consolidated.

Use the canonical current-state document instead:

- `docs/mpi_translator/mpi_translator_current_status.md`

Historical note:

- The shell-derived/operator-resident model remains the active MPI lowering
  model.
- P1-P6 are complete for the currently proven accepted surfaces.
- The accepted surfaces, fallback policy, source map, and TODO are now kept in
  the canonical current-status document.
